SynchBoard_FuncTest.c:
	Compiled as: functest
	Tests the functionality of SPI communications by talking to the EEPROM and reading the values of the DIP switches.

SynchBoard_SpeedTest.c:
	Compiled as: speedtest
	Tests the funcitonality of SPI communications at different speeds. Writes random data to the EEPROM and compares that
	to what is read back in order to determine if communication at a given speed works.

SynchBoard_FlashMem.c:
	Compiled as: flashmem
	Reads the ID of the flash memory chip.

compile:
	Compiles all of the C files above into their respective names as specified by 'Compiled as:'
